# 🚀 วิธี Deploy บน GitHub Pages

## ขั้นตอนการ Deploy

### 1. สร้าง GitHub Repository
```bash
# ไปที่ GitHub.com และสร้าง repository ชื่อ "valentine" (หรือชื่ออื่นๆ)
# แล้วรัน commands เหล่านี้ในโฟลเดอร์ d:\valentine
```

### 2. Push โค้ดขึ้น GitHub
```bash
git init
git add .
git commit -m "💕 Initial commit - Valentine's Day Photobooth"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/valentine.git
git push -u origin main
```

**⚠️ สำคัญ:** เปลี่ยน `YOUR_USERNAME` เป็นชื่อ GitHub account ของคุณ

### 3. เปิดใช้งาน GitHub Pages

1. ไปที่ repository บน GitHub
2. คลิก **Settings** (ในแถบด้านบน)
3. ไปที่ **Pages** (เมนูด้านซ้าย)
4. ใน **Source** เลือก **GitHub Actions**
5. GitHub Actions จะรัน automatic ทุกครั้งที่ push code

### 4. รอ Deployment เสร็จ

- ไปที่แท็บ **Actions** จะเห็น workflow กำลังรัน
- รอจน build และ deploy เสร็จ (ประมาณ 1-2 นาที)
- เมื่อเสร็จจะมีสถานะ ✅ สีเขียว

### 5. เข้าถึงเว็บไซต์

เว็บไซต์จะอยู่ที่:
```
https://YOUR_USERNAME.github.io/valentine/
```

## 🔧 การอัพเดทโค้ด

หลังจากแก้ไขโค้ดแล้ว:

```bash
git add .
git commit -m "✨ Update feature"
git push
```

GitHub Actions จะ deploy อัตโนมัติทันที!

## 📱 Custom Domain (Optional)

ถ้าต้องการใช้โดเมนของตัวเอง:
1. ไปที่ Settings → Pages
2. ใส่ Custom domain
3. อัพเดท DNS records ตามที่ GitHub บอก

## ⚙️ หมายเหตุสำคัญ

### Base Path Configuration
ไฟล์ `vite.config.js` ตั้งค่า base path เป็น `/valentine/`

ถ้าเปลี่ยนชื่อ repository ต้องเปลี่ยน base path ด้วย:

```js
export default defineConfig({
  plugins: [react()],
  base: '/your-repo-name/',
})
```

### Camera Permission
- GitHub Pages ใช้ HTTPS อัตโนมัติ ✅
- กล้องจะทำงานได้ปกติ ✅
- ผู้ใช้ต้อง Allow camera permission

## 🎉 เสร็จสิ้น!

แอป Valentine's Day Photobooth ของคุณพร้อมแชร์ให้คนพิเศษแล้ว! 💕

---

Made with ❤️ for Valentine's Day 2026
